# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: run_rebinning
version: master
environment: ubuntu
repository_url: https://github.com/icesat-2UT/PhoREAL.git
docker_url: mas.maap-project.org:5000/root/ade_base_images/r:${version}
# queue chosen when registering
memory: 15GB

# fill out these fields
# explain what this algorithm does
description: 
# path to the wrapper script for running the algorithm
run_command: /projects/above/gitlab_repos/PhoREAL/source_code/run_rebinning.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 10GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: input_atl03
    download: True
  - name: input_atl08
    download: True
  - name: output
    download: False